# Core ML/NLP Libraries
torch>=1.10.0,<2.0.0 # Specify a compatible PyTorch version range
transformers>=4.20.0,<5.0.0 # Specify a compatible Transformers version range
pandas>=1.3.0
numpy>=1.20.0
scikit-learn>=1.0.0

# Plotting Libraries
matplotlib>=3.4.0
seaborn>=0.11.0

# Optional: For Intel GPU users (if needed)
# intel_extension_for_pytorch>=1.12.0 # Uncomment if using IPEX

# Notes:
# - Version numbers are examples; adjust based on compatibility and project needs.
# - Ensure consistent versions across collaborators to avoid issues.
# - Consider using a virtual environment (e.g., venv, conda) to manage project dependencies.
